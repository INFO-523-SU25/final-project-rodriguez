{
  "hash": "501059e6c60e37e007a6dbffc99689ad",
  "result": {
    "engine": "jupyter",
    "markdown": "---\ntitle: Dataset Setup\n---\n\n\n::: {#load-pkgs .cell message='false' execution_count=1}\n``` {.python .cell-code}\nimport numpy as np\nimport pandas as pd\n\nfrom pathlib import Path\n\n# Modeling & preprocessing\nfrom sklearn.model_selection import train_test_split\nfrom sklearn.preprocessing import StandardScaler\nfrom sklearn.neighbors import NearestNeighbors\nfrom sklearn.mixture import GaussianMixture\n\n# Evaluation\nfrom sklearn.metrics import (\n    roc_auc_score, average_precision_score,\n    roc_curve, precision_recall_curve\n)\n\n# Plotting\nimport matplotlib.pyplot as plt\n```\n:::\n\n\n\n\n::: {#data-setup .cell execution_count=2}\n``` {.python .cell-code}\nRANDOM_STATE = 42\n\n# Load\ndf = pd.read_csv(\"data/creditcard.csv\")\n\n# Basic checks\nprint(\"Full shape:\", df.shape)\nprint(df.dtypes.head())\nprint(\"Total missing:\", df.isna().sum().sum())\n\n# Features/labels\nfeature_cols = [c for c in df.columns if c != \"Class\"]\nX = df[feature_cols].copy()\ny = df[\"Class\"].astype(int).copy()\n\n# Split (labels withheld for training)\nX_train, X_test, y_train, y_test = train_test_split(\n    X, y, test_size=0.30, stratify=y, random_state=RANDOM_STATE\n)\n\n# Scale Time and Amount using *train-only* fit, then transform both sets\nscaler = StandardScaler().fit(X_train[[\"Time\", \"Amount\"]])\nX_train[[\"Time\", \"Amount\"]] = scaler.transform(X_train[[\"Time\", \"Amount\"]])\nX_test[[\"Time\", \"Amount\"]]  = scaler.transform(X_test[[\"Time\", \"Amount\"]])\n\n# Quick summary\nprint(\"Train shape:\", X_train.shape, \"| Test shape:\", X_test.shape)\nprint(f\"Fraud rate (train): {y_train.mean():.6f}  ({100*y_train.mean():.3f}%)\")\nprint(f\"Fraud rate (test):  {y_test.mean():.6f}  ({100*y_test.mean():.3f}%)\")\n\nprint(y_train.value_counts().to_dict(), y_test.value_counts().to_dict())\n\nassert \"Class\" not in X_train.columns and \"Class\" not in X_test.columns\n```\n\n::: {.cell-output .cell-output-stdout}\n```\nFull shape: (284807, 31)\nTime    float64\nV1      float64\nV2      float64\nV3      float64\nV4      float64\ndtype: object\nTotal missing: 0\nTrain shape: (199364, 30) | Test shape: (85443, 30)\nFraud rate (train): 0.001725  (0.173%)\nFraud rate (test):  0.001732  (0.173%)\n{0: 199020, 1: 344} {0: 85295, 1: 148}\n```\n:::\n:::\n\n\n## Plots\n\n",
    "supporting": [
      "code_files"
    ],
    "filters": [],
    "includes": {}
  }
}